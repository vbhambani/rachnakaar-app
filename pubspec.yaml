name: rachnakaar
description: Rachnakaar — Stories that touch the heart. Android companion app for rachnakaar.com.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  url_launcher: ^6.2.2
  cached_network_image: ^3.3.0
  html_unescape: ^2.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
